import React from "react";
import PropTypes from "prop-types";
import "./style.css";

const Header = (props) => {
	return (
		<div>
			<h1>Try Not To Smile - Challenge</h1>
		</div>
	);
};

Header.propTypes = {};

export default Header;
